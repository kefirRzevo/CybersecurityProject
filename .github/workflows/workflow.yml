name: Steganography post-push
on:
  push:
      paths:
        - src/**
      branches:
        - main
jobs:
  main:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
    - uses: actions/checkout@v3
    - name: Wav LSB
      run: cd tests/test_wav_lsb && python3 TestWavLSB.py
    - name: Video Parse
      run: cd tests/test_video_parse && python3 TestVideoParse.py
